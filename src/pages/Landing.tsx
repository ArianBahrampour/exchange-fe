import { Card, Typography } from "@mui/material";

const LandingPage = () => {
    return (
        <div>
            <Card>
                <Typography>asd</Typography>
            </Card>
        </div>
    );
};

export default LandingPage;
